package de.keksuccino.konkrete.util.rendering.ui;

/** Captures the immutable state needed for press. */
public enum PressState {
    /** Selects pressed behavior. */
    PRESSED,
    /** Selects released behavior. */
    RELEASED
}
