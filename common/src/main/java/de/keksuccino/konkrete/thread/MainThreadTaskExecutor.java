package de.keksuccino.konkrete.thread;

import org.jetbrains.annotations.NotNull;

public class MainThreadTaskExecutor {

    public static void executeInMainThread(@NotNull Runnable task) {
        if (Servi)
    }

}
